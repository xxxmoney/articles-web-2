import { defineStore } from 'pinia'
import axios from 'axios'

export const useStore = defineStore('example-store', {
    state: () => {
        return {
            loaded: false
        }
    },

    getters: {
        
    },

    actions: {
        
    }
})